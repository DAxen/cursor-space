# 知识工程建设进展报告

> 报告状态：阶段性进展稿  
> 统计范围：当前已完成分类治理的知识库  
> 统计时间：2026 年 8 月  
> 数据基线：605 个知识文件

## 1. 执行摘要

当前知识工程建设已从“使用 OKL 自动生成知识”进入“面向领域结构的人机协同治理”阶段。

前期通过公司内部知识库管理 Skill——OKL，对存量代码进行分析，已经具备：

- 从代码逆向提取代码调用流程；
- 从设计文档正向摄取知识；
- 面向知识库提供查询能力。

在实际使用中发现，知识生成能力并不等同于完整的知识工程能力：

1. 业务意图、架构决策、设计原因、规范约束等无法仅从代码逆向恢复；
2. 从文档正向摄取的知识以平铺形式存在，缺少领域结构、上下位关系及知识间逻辑联系；
3. 平铺知识不利于人类理解、评审和持续维护，也不利于 AI Agent 准确定位和组合上下文。

针对上述问题，团队改进了 OKL Skill，新增了面向既有知识库的分类治理能力：

- 从现有知识中提取业务领域概念；
- 以交互方式由领域专家确认领域划分；
- 由人类判断层次关系、概念归属是否合理；
- 基于新的领域知识模板对内容重新归类；
- 将平铺知识逐步转换为可链接、可追溯、可独立维护的知识页面；
- 建立人和 AI 共同治理知识库的基础。

目前已完成 **605 个知识文件**的分类治理。其中：

- `implement/` 代码实现知识 520 个，占 86.0%；
- `architecture/` 架构知识 49 个，占 8.1%；
- `domains/` 领域知识 25 个，占 4.1%；
- `decisions/` 决策知识 6 个，占 1.0%；
- `conventions/` 规范与约束知识 5 个，占 0.8%。

阶段性结论是：

> 已初步建立从 OKL 原始知识库向“领域知识、架构知识、实现知识、决策知识、规范与约束”分层知识库转化的治理路径，并完成首批 605 个文件的落位。

但当前数据只能证明**知识分类治理和结构迁移已经发生**，尚不能单独证明知识正确率、完整率、链接质量或编码 Agent 应用效果已经提升。下一阶段应围绕知识质量测评、知识调试和真实编码任务建立量化证据。

---

## 2. 建设背景

### 2.1 原有 OKL 能力

OKL 是公司内部的知识库管理 Skill，承担知识生产和查询入口的角色。

当前能力包括：

```text
存量代码
   ↓ 逆向分析
代码调用流程知识
   ↓
OKL 知识库 ← 正向摄取 ← 设计文档
   ↓
知识查询
```

其主要价值是降低存量项目知识构建门槛：

- 自动分析代码调用关系；
- 将部分隐含在代码中的实现信息转化为可查询内容；
- 摄取已有设计文档，减少知识重复录入；
- 为人和编码 Agent 提供统一查询入口。

### 2.2 实践中发现的问题

#### 2.2.1 代码逆向存在知识边界

代码主要表达“系统如何实现”，但无法完整表达“为什么这样实现”。

仅依赖代码逆向通常难以恢复：

- 业务概念和业务规则的完整定义；
- 产品行为背后的业务意图；
- 架构方案的选择原因；
- 被否决的备选方案；
- CCB、NodePC、DRB 等评审决策；
- 公司或领域编码规范；
- 架构约束和 API 使用限制；
- 历史背景及适用范围。

因此，代码逆向知识应被视为知识库的重要组成部分，但不能替代业务、架构、决策和规范知识。

#### 2.2.2 正向摄取形成平铺知识

从设计文档正向摄取后，知识页面缺少统一的领域模型和组织结构，主要表现为：

- 知识页面以文档来源为中心，而不是以领域概念为中心；
- 页面之间缺少上下位关系；
- 同一概念可能散落在多个文档页面中；
- 架构、实现、决策和规范知识混杂；
- 页面间逻辑关系不明确；
- 人类难以形成整体认知；
- AI Agent 难以判断应优先读取哪些知识。

#### 2.2.3 自动生成结果缺少共治入口

如果知识库仅由 AI 自动生成，人类只能被动阅读，很难持续参与：

- 领域边界校准；
- 概念命名统一；
- 上下位关系调整；
- 重复内容合并；
- 错误知识纠正；
- 过期知识归档；
- 缺失知识补齐。

因此，需要把“人类决策”嵌入知识治理流程，而不是只在知识生成完成后进行零散修正。

---

## 3. 改进方案

### 3.1 建设思路

本阶段的改进目标，是将 OKL 从“知识抽取与查询工具”增强为“知识抽取、结构治理和人机共治工具”。

总体流程为：

```text
代码逆向 / 文档摄取
          ↓
     OKL 原始知识库
          ↓
   提取业务领域概念
          ↓
AI 推荐领域划分和层次关系
          ↓
领域专家交互确认、修改、合并或拆分
          ↓
按领域知识模板进行分类治理
          ↓
页面原子化、关系链接、来源追溯
          ↓
人和 AI 持续共治
```

### 3.2 基础知识结构

当前治理知识库采用以下基础结构：

| 知识层级 | 主要内容 | 当前目录 |
|---|---|---|
| 领域知识 | 业务知识、领域业务术语、产品手册 | `domains/` |
| 架构知识 | 架构设计、特性设计、实现设计 | `architecture/` |
| 实现知识 | 模块实现、代码流程、接口与实现细节 | `implement/` |
| 决策知识 | CCB、NodePC、DRB 等评审决策及原因 | `decisions/` |
| 规范与约束 | 公司/领域编码规范、架构约束、API 使用约束 | `conventions/` |

代码符号索引、跨文件引用、类型层次和调用图等动态代码事实，可继续由代码分析或 clangd/LSP 等结构化工具实时提供，避免将高频变化的代码事实全部手工固化为静态文档。

### 3.3 人机共治机制

新的 OKL Skill 不直接把 AI 推荐的领域结构视为最终结果，而是提供交互式治理流程：

1. AI 从知识页面中识别领域概念；
2. AI 推荐领域归属和层次关系；
3. 领域专家确认领域划分是否正确；
4. 领域专家判断层次关系是否合理；
5. 对错误结构进行重命名、合并、拆分或移动；
6. 使用确认后的领域模板重新组织知识；
7. 将治理结果沉淀为后续自动分类的规则和上下文。

该机制将职责划分为：

- **AI 负责规模化提取、推荐和机械整理**；
- **人类负责领域边界、语义正确性和最终决策**。

---

## 4. 阶段性成果

### 4.1 治理知识库总览

| 层级 | 文件数 | 占比 | 来源策略 |
|---|---:|---:|---|
| `domains/` | 25 | 4.1% | split（OKL 域页 → 5 文件目录） |
| `architecture/` | 49 | 8.1% | move / rewrite / merge / split 混合 |
| `implement/` | 520 | 86.0% | mirror（直接复制） |
| `decisions/` | 6 | 1.0% | move + ADR 模板改写 |
| `conventions/` | 5 | 0.8% | split + 外部文档提取 |
| **合计** | **605** | **100%** |  |

> 注：占比依据文件数重新计算并四舍五入至一位小数。原始数据中 `implement/` 标注为 85.9%；按 `520 ÷ 605` 计算为 85.95%，规范四舍五入后为 86.0%。文件数合计为 605。

### 4.2 数据结构分析

#### 4.2.1 实现知识构成当前知识库主体

`implement/` 共 520 个文件，占 86.0%，说明现有知识资产主要来源于代码和实现层。

这一结果与前期使用 OKL 进行存量代码逆向的建设路径一致：

- 代码是当前最充足、最容易自动获取的知识源；
- 调用流程和实现细节已经形成较大规模；
- 代码逆向能力已成为知识库当前的主要供给能力。

同时，这一比例也说明知识库仍具有明显的“实现知识重、非代码知识轻”特征。

#### 4.2.2 非实现知识已开始形成独立结构

除 `implement/` 外，其余四类知识共 85 个文件，占约 14.0%：

| 非实现知识 | 文件数 | 占总知识库 |
|---|---:|---:|
| 领域知识 | 25 | 4.1% |
| 架构知识 | 49 | 8.1% |
| 决策知识 | 6 | 1.0% |
| 规范与约束 | 5 | 0.8% |
| **合计** | **85** | **约 14.0%** |

这 85 个文件代表治理工作已开始将原本平铺或混杂的知识分离为更清晰的知识类型。

#### 4.2.3 架构知识治理深度相对较高

`architecture/` 使用 move、rewrite、merge、split 等混合策略，而非简单复制，说明架构知识已经发生了较明显的结构化改造：

- 将原页面移动到合理领域；
- 对内容进行重写；
- 合并重复或高度相关内容；
- 拆分包含多个主题的页面。

从治理策略看，架构知识是当前除领域知识外治理动作最丰富的部分。

但仍需要通过抽检确认：

- 重写是否准确保留原意；
- 合并是否造成信息丢失；
- 拆分粒度是否符合原子化要求；
- 架构知识是否能追溯到原始设计或代码。

#### 4.2.4 领域知识已从域页向模板化目录转化

`domains/` 采用 split 策略，将 OKL 原有域页转换为五文件目录结构。

该动作表明领域知识不再只是单一汇总页面，而开始采用模板化结构承载不同类型的领域信息，有助于：

- 增强领域知识层次感；
- 让知识页面职责更加单一；
- 为后续页面全链接提供稳定节点；
- 便于人类逐项确认；
- 便于 Agent 按任务选择性加载。

目前应进一步统计：

- 已形成多少个领域目录；
- 每个目录五类文件的完整率；
- 领域目录之间是否存在重叠；
- AI 建议经人工直接接受、微调或重构的比例。

#### 4.2.5 决策和规范知识规模仍较小

`decisions/` 和 `conventions/` 合计 11 个文件，占总知识库 1.8%。

这可能由以下因素导致：

1. 此类知识确实数量较少；
2. 决策和规范来源尚未全部接入；
3. 相关知识仍混在架构或实现页面中；
4. 历史评审记录尚未结构化；
5. 公司或领域规范仍位于外部系统。

仅凭文件数量不能断言覆盖不足，但该分布提示下一阶段应优先验证：

- CCB、NodePC、DRB 等历史决策覆盖率；
- 公司及领域编码规范摄取范围；
- 架构约束覆盖率；
- API 使用约束覆盖率；
- 决策和约束是否能够被编码 Agent 正确召回。

### 4.3 来源策略分析

当前采用多种治理策略：

| 策略 | 含义 | 主要价值 | 主要风险 |
|---|---|---|---|
| mirror | 直接镜像或复制原知识 | 快速保留存量内容 | 原有平铺和粒度问题可能被保留 |
| move | 调整知识所属目录 | 修正分类和领域归属 | 内容本身未必得到改善 |
| rewrite | 按模板重新表达 | 提升一致性和可读性 | 可能改变原意或遗漏细节 |
| merge | 合并重复或相关知识 | 减少冗余、形成完整主题 | 来源冲突可能被掩盖 |
| split | 拆分复合页面 | 支持原子化和独立引用 | 拆分不当可能割裂上下文 |
| 外部提取 | 从代码外文档摄取知识 | 补充代码无法逆向的内容 | 来源版本和有效期需要治理 |

当前 520 个实现文件采用 mirror，占总量 86.0%。这有利于快速完成首轮迁移，但也意味着：

> 当前“已分类落位”不应直接等同于“已完成深度治理”。

后续应区分：

- 已迁移文件；
- 已分类文件；
- 已按模板改写文件；
- 已原子化文件；
- 已建立有效关系的文件；
- 已通过专家质量测评的文件。

---

## 5. 已形成的能力

基于当前工作，可确认已形成以下阶段性能力：

### 5.1 知识来源双通道

- 代码逆向：提取实现流程和代码关系；
- 文档正向摄取：补充业务、架构、决策和约束知识。

### 5.2 领域概念提取

能够从 OKL 生成的知识库中识别业务领域概念，为领域建模提供候选结果。

### 5.3 交互式领域确认

领域专家可以参与：

- 领域划分确认；
- 概念归属调整；
- 上下位关系修正；
- 领域节点合并和拆分。

### 5.4 模板化分类治理

知识可以按照领域、架构、实现、决策和规范约束进行分类，并针对不同类型应用 mirror、move、rewrite、merge、split 等策略。

### 5.5 人机共治基础

已经形成“AI 提取和推荐、人类确认和决策、系统执行治理”的基本协作模式。

---

## 6. 当前阶段判断

### 6.1 已完成或已有数据支持

| 结论 | 证据状态 |
|---|---|
| 已完成首批知识分类治理 | 605 个文件已按五类知识落位 |
| 实现知识是当前主体 | 520 个文件，占 86.0% |
| 已采用多种治理策略 | 已记录 mirror、move、rewrite、merge、split 等策略 |
| 已开始补充代码外知识 | 决策知识和规范约束包含文档提取和模板改写 |
| 已形成交互式领域治理方向 | 改进后的 OKL 支持领域提取和人工确认 |

### 6.2 尚待数据验证

以下目标目前不能仅凭文件分布证明：

| 目标 | 待补充证据 |
|---|---|
| 领域划分正确 | 专家标注准确率、直接接受率、微调率、重构率 |
| 层次关系合理 | 专家评分、领域重叠率、层级调整记录 |
| 页面全链接 | 孤立页面率、断链率、关系准确率、连通分量 |
| 全链路原子化 | 原子页面占比、单页知识点数、独立引用率 |
| 知识完整 | 五类知识覆盖度、标准知识清单命中率 |
| 知识正确 | 专家抽检正确率和来源可追溯率 |
| 有利于人类使用 | 查找时间、导航跳数、用户反馈 |
| 有利于 AI 使用 | Recall@K、问答正确率、引用准确率 |
| 提升编码质量 | 真实任务首次通过率、返工轮次、约束违规数 |
| 降低治理成本 | 每百页人工工时、AI 建议接受率 |

---

## 7. 主要问题与风险

### 7.1 知识结构不均衡

实现知识占 86.0%，而决策和规范知识仅占 1.8%。如果后者覆盖不足，编码 Agent 即使理解“如何实现”，仍可能不知道：

- 为什么选择当前方案；
- 哪些设计不能修改；
- 哪些 API 禁止或受限使用；
- 哪些公司或领域规范必须遵守。

### 7.2 Mirror 内容可能保留原有问题

520 个实现文件直接 mirror，可以快速完成迁移，但可能继续保留：

- 页面过大；
- 一个页面包含多个知识点；
- 重复描述；
- 缺少来源；
- 缺少与领域、架构和决策知识的链接。

### 7.3 AI 领域划分仍需质量基线

交互式确认机制已经建立，但尚需记录：

- AI 推荐的准确率；
- 人工直接接受率；
- 人工修改类型；
- 人工治理耗时；
- 不同领域的稳定性。

否则无法判断该能力是否具备规模化价值。

### 7.4 全链接可能演变为无效链接

链接数量增加不等于知识关系质量提高。需要避免：

- 为实现“全链接”而建立弱相关链接；
- 链接类型不明确；
- 页面互相引用但没有逻辑语义；
- 来源链接与知识关系链接混杂；
- 断链和过期链接无法发现。

### 7.5 知识与代码可能发生版本漂移

代码、设计和知识库独立更新时，可能出现：

- 代码已变更但知识未刷新；
- 设计文档已更新但领域页面仍为旧版本；
- 决策已失效但仍被 Agent 使用；
- API 约束变更后未及时传播。

需要建立增量刷新、知识测评和版本追踪机制。

---

## 8. 下一阶段重点

### 8.1 建立知识资产台账

在现有文件数基础上，补充：

- 各类知识事实数；
- 领域目录数；
- 各领域文件分布；
- 来源文档和代码版本；
- 页面创建及更新时间；
- 页面状态和责任人；
- 采用的治理策略；
- 是否经过人工确认；
- 是否通过质量测评。

### 8.2 建立知识质量测评

优先测量：

1. 内容正确率；
2. 内容完整率；
3. 来源可追溯率；
4. 领域分类准确率；
5. 原子页面占比；
6. 孤立页面率；
7. 断链率；
8. 关系准确率。

详细方法见《知识库应用效果评估指南》。

### 8.3 建立知识调试能力

在 OKL、Claude Code 等消费端记录：

- 哪些知识被召回；
- 哪些页面被实际打开；
- 哪些知识被引用；
- 哪些知识影响了回答或编码决策；
- 哪些查询无结果；
- 哪些回答被用户纠正。

据此识别：

- **热点知识**：继续细化、拆分和深挖；
- **缺失知识**：补充知识源并触发刷新；
- **低效知识**：被频繁召回但很少采用，需优化链接或内容；
- **零调用知识**：人工判断保留、调整入口、合并或归档。

### 8.4 建立知识刷新闭环

```text
新增代码或设计文档
        ↓
触发知识增量更新
        ↓
分类、原子化和关系刷新
        ↓
知识质量测评
        ↓
发布给人和 Agent 使用
        ↓
消费反馈与知识调试
        ↓
再次触发知识刷新
```

刷新触发器包括：

- 新特性设计文档；
- 新模块设计文档；
- 代码或接口变更；
- CCB、NodePC、DRB 新决策；
- 规范和 API 约束更新；
- 知识测评发现问题；
- 消费端反馈错误；
- 知识调试发现热点或缺失。

### 8.5 验证 Agent 应用效果

建议建立 A/B/C 对照实验：

| 实验组 | Agent 可使用的知识 |
|---|---|
| A 组 | 代码和原始文档 |
| B 组 | 代码、原始文档和治理前 OKL 知识库 |
| C 组 | 代码、原始文档和治理后知识库 |

重点比较：

- 首次实现通过率；
- 编译和测试通过率；
- 规范及架构约束违规数量；
- API 误用数量；
- Code Review 问题数量；
- 返工轮次；
- 任务完成时间；
- Token 消耗；
- 有效知识使用率。

---

## 9. 建议补充的核心数据

为形成正式成果报告，建议优先补充以下数据：

| 优先级 | 数据 | 用途 |
|---|---|---|
| P0 | 领域目录数量及五文件模板完整率 | 证明领域治理完成度 |
| P0 | AI 分类直接接受率、微调率、重构率 | 证明人机共治质量 |
| P0 | 每百页人工治理工时 | 评估规模化成本 |
| P0 | 原子页面占比 | 证明原子化目标 |
| P0 | 孤立页面率、断链率、关系准确率 | 证明全链接质量 |
| P0 | 专家抽检正确率、完整率、可追溯率 | 证明知识内容质量 |
| P0 | 查询 Recall@5、回答正确率、引用准确率 | 证明知识可消费 |
| P0 | 真实编码任务首次通过率和返工轮次 | 证明工程价值 |
| P1 | 新知识源刷新成功率和刷新时延 | 证明知识保鲜能力 |
| P1 | 热点、缺失、零调用知识统计 | 证明知识调试能力 |
| P1 | 决策和规范知识覆盖率 | 验证当前结构短板 |

---

## 10. 阶段里程碑

| 里程碑 | 状态 | 阶段成果 |
|---|---|---|
| M1：OKL 代码逆向和文档摄取 | 已具备 | 可生成代码流程知识、摄取设计文档并查询 |
| M2：识别原始知识库问题 | 已完成 | 明确代码逆向边界和平铺知识问题 |
| M3：领域概念提取及交互确认 | 已实现能力 | 支持人类确认领域划分和层次关系 |
| M4：知识分类治理 | 已完成首批 | 605 个文件按五类知识落位 |
| M5：页面全链接及原子化 | 建设中/待量化 | 需要关系和原子化指标验证 |
| M6：知识质量测评 | 待建设 | 建立专家标注集和自动回归 |
| M7：知识调试和运营看板 | 待建设 | 统计热点、缺失和零调用知识 |
| M8：Agent 应用效果验证 | 待建设 | 用真实问答、设计和编码任务验证收益 |

---

## 11. 总结

本阶段最重要的进展，是知识工程建设目标已经从“尽可能多地自动生成知识”，升级为“构建具有领域结构、明确知识类型和人机共治机制的知识库”。

基于改进后的 OKL Skill，团队已经：

- 保留代码逆向和文档正向摄取能力；
- 识别并正视代码无法恢复全部知识的客观边界；
- 引入业务领域概念提取；
- 建立人类参与领域划分和层次确认的交互机制；
- 按领域、架构、实现、决策、规范与约束完成首批知识分类；
- 形成 605 个文件的治理知识库。

当前知识库中 86.0% 为实现知识，反映出已有建设基础以代码逆向为主；领域和架构知识已经开始结构化，决策及规范约束知识则是下一阶段需要重点验证和补强的部分。

下一阶段应从“结构建设”转向“效果证明”，重点完成：

1. 量化领域分类和人机共治质量；
2. 验证页面原子化和链接关系；
3. 建立知识质量测评和增量刷新闭环；
4. 建设知识调用可观测性；
5. 用真实问答、设计和编码任务验证工程收益。

在完成上述验证前，最准确的阶段性表述是：

> 已完成知识治理体系的关键能力建设和首批 605 个文件的结构化落位，初步形成领域划分、分类治理和人机共治路径；知识内容质量、链接质量及 Agent 应用效果将进入下一阶段量化验证。
